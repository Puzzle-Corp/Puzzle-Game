const db=require("../models");

module.exports={
    findGamesByCatName:function(req,res){
       // db.Game



        // db.Category.find({parentCatName:req.params.parentCatName})
        // .then(dbModel => res.json(dbModel))
        // .catch(err => res.status(422).json(err));
    }
};